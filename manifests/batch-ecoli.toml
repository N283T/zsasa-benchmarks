# Batch benchmark manifest placeholder.
# Existing comparator outputs may be reused for first archive/preprint, but no rerun is scheduled here yet.

id = "batch-ecoli-existing-comparator-baseline"
status = "planning"

[dataset]
id = "UP000000625_83333_ECOLI_v6_pdb"
expected_count = 4370
historical_path = "/Users/nagaet/pdb/afdb/UP000000625_83333_ECOLI_v6/pdb"
role = ["batch-throughput"]

[historical_results]
results_dir = "/Users/nagaet/freesasa-zig/benchmarks/results/batch/128/ecoli_t10"
policy = "reuse existing comparator timings unless a full benchmark rerun is explicitly approved"

[planned_refresh]
rerun_zsasa_only = true
rerun_comparators = false
n_points = 128
threads = [10]
runs = 10
warmup = 3
